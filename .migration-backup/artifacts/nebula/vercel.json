{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build && rm -rf public && mkdir -p public && cp -R dist/public/. public/",
  "outputDirectory": "public"
}
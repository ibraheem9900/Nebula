{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/nebula run build && rm -rf public && mkdir -p public && cp -R artifacts/nebula/dist/public/. public/",
  "outputDirectory": "public"
}